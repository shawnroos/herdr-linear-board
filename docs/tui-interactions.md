# TUI interaction contract

This is the behavior-preservation source of truth for the functional UI redesign. Every listed capability must remain available through its existing keyboard path. Mouse/touch-first controls may expose the same capability, but must dispatch the same reducer/effect path and must not add product features.

## Layout vocabulary

- **Compact:** terminal width `< 60`; board shows exactly one full-width column and a four-row mobile header (identity/count, board + visibility controls, column navigator, divider). The navigator shows `(M)`/`(A)` beside the column and does not repeat the global running count. Sheets use only the board content region and receive a clickable `[ X ]`.
- **Regular:** width `60..=119`; multi-column board and centered content-region sheets, with the complete header on one line.
- **Wide:** width `>= 120`; same interaction model as Regular, with more columns/space and full filter names.
- Every layout reserves persistent top chrome and a bottom board action rail. There is no permanent footer hint row; a transient toast may use one row immediately above the action rail without changing idle geometry. Overlays and fullscreen detail/forms never cover the rails. Toasts expire after more than four seconds.
- Every visible button is an exact transparent `[ NAME ]` chip: only the chip cells paint bold white text and brackets; selected chips add an underline, and the surrounding background remains visible. Status cards retain semantic colors; primary/destructive action tone does not recolor buttons.
- In the tables, **❌ no mouse parity** means the keyboard capability cannot be performed by a corresponding mouse control. “Partial” calls out narrower mouse support.

## Interaction matrix

| Screen/context | Capability | Keyboard | Mouse | Compact behavior | Backing effect / test evidence |
|---|---|---|---|---|---|
| Global, all non-form screens | Open contextual help and return to the exact originating screen | `?` | **✓** contextual `[ ? Help ]` action-rail chip | Content-region sheet; `[ X ]` closes | `app/mod.rs::on_key`; `tests/update/modals.rs::help_is_reachable_from_every_non_form_screen`, `help_returns_to_the_screen_it_was_opened_from`; `tests/mouse.rs::idle_board_footer_is_inert_and_help_stays_in_the_header_and_action_row` |
| Global refresh/event loop | Live board updates; boardd replacement reconnects both event and request sockets, then refetches the selected board; injected time refreshes running timers; terminal resize/editor round-trip forces full redraw | No direct key for event refresh | none | Same | `Msg::Refresh -> Effect::Refetch`; `runtime.rs::event_loop`; live crash/restart regression; `tests/update/editor.rs::*redraw*`; `tests/snapshots.rs::active_run_summary_wins_over_card_updated_at_for_timer` |
| Board | Focus previous/next column, wrapping at ends | `←`/`→`, `h`/`l` | Compact: click `‹`/`›`; Regular/Wide: click a column header or a card in that column. **Partial parity:** mouse has no generic off-screen prev/next control in Regular/Wide | One column only; header arrows wrap | `app/board.rs`, `app/nav.rs`, `Zone::HeaderPrev/Next`; `tests/update/support_nav.rs::column_navigation_wraps`; `tests/mouse.rs::compact_board_header_zones_present_and_wrap_both_ends` |
| Board | Focus previous/next card within focused column, wrapping | `↑`/`↓`, `k`/`j` | Single-click a visible card; wheel can pull selection into viewport. **Partial parity:** cannot mouse-select a non-rendered card except by wheel first | Cards have variable 4/5-row boxed heights; one column | `App::move_card`; `mouse.rs`; `tests/update/support_nav.rs::card_navigation_wraps_within_column`; mouse wheel tests |
| Board | Switch active project | `p` | Click the header `Project: <name>` chip (every layout) | Project picker at every breakpoint: current project first, then recents (≤3), then the remaining projects alphabetically with `Global` last, then a `＋ New project` row. Choosing another project opens that project's board picker — a board choice is forced before the context switches | `Effect::LoadProjectPicker` → `SelectProject`; `Effect::ProjectCreate` for the new-project form; `tests/update/scope.rs::p_opens_project_picker_with_current_project_first`, `choosing_another_project_opens_its_board_picker_and_selects_only_on_board_enter`, `picker_new_project_and_new_board_rows_open_the_create_forms`, `project_create_form_submission_emits_project_create`; `tests/mouse.rs` header chip zones |
| Board | Switch active board | `b` | Click the header `Board: <name>` chip (every layout) | Board picker at every breakpoint (Compact `b` no longer routes through the columns switcher). Picker order: the project's selected (or first) board, then recents (≤3), then the remaining boards alphabetically, then `⇄ Other projects…` (drills to the project picker) and `＋ New board`. Choosing a board selects it (and its project) | `Effect::LoadBoardPicker` → `SelectBoard`; `Effect::BoardCreate` for the new-board form; `tests/update/support_nav.rs::board_picker_loads_and_switch_preserves_filter`, `tests/update/switcher.rs::b_in_compact_opens_the_board_picker`, `tests/update/scope.rs::b_opens_board_picker_and_other_projects_drills_to_the_project_picker`; `tests/mouse.rs` header chip zones |
| Board | Create card in focused column | `n` | **❌ no direct mouse parity** (form itself has mouse Save/Cancel) | Fullscreen form | Opens `Form::card_create_with_session`, `Effect::LoadFormOptions`; `tests/update/forms.rs::opening_card_form_fetches_capabilities_and_spaces` |
| Board | Create column | `N` | **❌ no direct mouse parity** | Fullscreen form | `Form::column_create`, `Effect::LoadFormOptions`; `tests/update/forms.rs::opening_column_form_loads_only_column_metadata` |
| Board | Edit selected card | `e` | **❌ no mouse parity** (Board cards have no Edit control) | Fullscreen form | `Form::card_edit`, `Effect::LoadFormOptions`; snapshot `edit_card_modal`; `tests/mouse.rs::board_cards_have_no_edit_delete_controls_or_mouse_hit_zones` |
| Board | Edit focused column | `E` | **❌ no direct mouse parity** | Fullscreen form | `Form::column_edit`, `Effect::LoadFormOptions`; column form tests/snapshots |
| Board | Archive active card / restore archived card | `a` | **❌ no mouse parity** | Same | `Effect::CardArchive`; `tests/update/scope.rs::archive_shortcut_archives_and_restores_selected_card` |
| Board | Cycle card visibility `ACTIVE → ALL → ARCHIVED → ACTIVE`; reset card selection and update pane title | `v` | **✓** direct `[ Active ]` `[ All ]` `[ Archived ]` chips, independent of order | Same; every layout exposes the three filters; selected chip is bold/underlined without a background | `Effect::SetPaneTitle`; `tests/update/support_nav.rs::archive_filter_defaults_active_and_cycles_all_then_archived`; pane-title tests |
| Board | Delete selected card after confirmation | `d`, then `y`/`Enter` or `n`/`Esc` | **❌ no mouse parity** (Board cards have no Delete control) | Confirmation stays in the content region | `ConfirmPurpose::DeleteCard` → `Effect::CardDelete`; `tests/mouse.rs::board_cards_have_no_edit_delete_controls_or_mouse_hit_zones`; confirm/mouse sheet-close tests |
| Board | Delete focused column; empty column confirms directly; populated column first picks relocation target then confirms | `D`; picker `↑/↓ k/j`, `Enter`; confirm `y`/`Enter` or `n`/`Esc` | **❌ no picker-row or yes/no mouse parity**; Compact `[ X ]` cancels sheets | Fullscreen picker/confirm; long picker options wrap and window around selection | `Effect::ColumnDelete`; `tests/snapshots.rs::delete_column_with_cards_picker`; `tests/update/modals.rs::deleting_a_column_with_cards_confirms_after_the_destination_picker` |
| Board | Move card to another column on same board, or to a board of another project | `m`; form fields cycle Project → Board → Column → Position | **❌ no field mouse parity**; Compact `[ X ]` cancels | Move form (Project/Board/Column/Position) at every breakpoint; changing the Board reloads the Column options; the move **never** changes the persistent selection or recency | `Effect::CardMove`, `LoadProjects`, `LoadMoveColumns`; snapshots `move_card_flow`, `move_card_cross_board_via_b_toggle` |
| Board | Stage and commit focused-column reorder mini-mode | `M`; `←/→ h/l` clamp at edges; `Enter` commits; `q`/`Esc` discards | Regular/Wide can drag a column header and drop on another visible column (immediate, no staging). **Compact: ❌ no reorder-drag parity** because the header is consumed by identity/navigation and only one column is visible; `[ X ]` only cancels mini-mode | Content-region instruction sheet overlays board while staged permutation remains visible underneath | `Effect::ColumnReorder`; `tests/update/scope.rs::move_column_*`, `column_drag_produces_reorder`; snapshot `move_column_mini_mode` |
| Board | Stage and commit in-column card reorder mini-mode | `O`; `j/k ↑/↓` clamp at edges (no wrap); `Enter` commits one same-column `card.move` with the staged position; `q`/`Esc` discards; selection follows the staged card | Drag a card onto another card row of the same column (drop places it on the hovered card's position; drop below the last visible card appends; drop on its own row is a no-op). **Compact: same-column drag remains the only usable card drag** with one visible column | Content-region instruction sheet overlays board while the staged order remains visible underneath | `Effect::CardMove` with `position`; `app/reorder_card.rs`, `app/drag.rs` (`drag_hover_card`); `tests/update/scope.rs::reorder_card_*`, `drag_card_within_same_column_reorders`, `drag_card_dropped_on_own_slot_is_noop`; `tests/mouse.rs::drag_card_onto_another_card_in_the_same_column_reorders`; snapshots `reorder_card_mini_mode` |
| Board | Shove selected card one column left/right, wrapping | `H` / `L` | Drag visible card across visible columns. **Partial parity:** Compact has only one visible destination, and Regular/Wide cannot drag to an off-screen column | Keyboard still works in Compact | `Effect::CardMove`; `tests/update/scope.rs::shove_moves_card_and_focus`, `drag_card_to_other_column_produces_move` |
| Board | Open selected card detail | `Enter` | Double-click the exact visible card cell within `<400ms` | Detail popup/fullscreen uses only the board content region at Compact; state remains toggleable | `Effect::LoadDetail`; `App::open_detail`; detail tests and `mouse.rs` |
| Board | Apply built-in `pipeline` template, empty-board only | `T` | Compact only: center switcher → “Apply template”. **❌ Regular/Wide mouse parity** | Switcher row stays open and toasts if board is nonempty; closes on success | `Effect::TemplateApply("pipeline")`; `tests/update/switcher.rs::*apply_template*`; `tests/update/support_nav.rs::template_only_on_empty_board` |
| Board | Manual refresh | `r` / `R` | **❌ no mouse parity** | Same | Immediate non-error `refreshed` toast then `Effect::Refetch`; `tests/update/support_nav.rs::r_refreshes_board_with_toast`, `r_reloads_through_the_driver` |
| Board | Quit TUI | `q` / `Esc` | **❌ no mouse parity** | Same | `Effect::Quit`; `tests/update/support_nav.rs::help_and_quit` |
| Board mouse | Focus/drag card; dropping in origin is no-op; dropping in the same column reorders to the hovered card's position | keyboard equivalents above | Left-down begins drag (unless archived), drag hover changes highlighted target and hovered card row, left-up emits a same-column `card.move` with `position` (or a cross-column move); double-click also begins detail | Cross-column card drop is practically unavailable with one visible column; same-column reorder-drag works | `app/drag.rs`, `app/mouse.rs`; `tests/update/scope.rs::drag_*`; `tests/mouse.rs::drag_card_*` |
| Board mouse | Focus/drag column header | `M` mode is keyboard analogue | Left-down header begins drag; drag/drop emits reorder | Header controls consume row 1; one-column layout prevents destination | `app/drag.rs`; `column_drag_produces_reorder` |
| Board mouse | Scroll one hovered column’s cards without reordering | Board card nav is keyboard analogue | Wheel up/down clamps per-column offset; if hovered column is selected, selection is pulled into new visible window | Works on the single compact column; scrollbar shown when overflowing | `mouse.rs::scroll_hovered_column`; `tests/mouse.rs::wheel_scrolls_*`, zero-slot test |
| Board visible content | Read column name, visible-card count, trigger; read card title, status glyph/label, harness/model, running elapsed time; archived marker; selected/drag target styling; scrollbars | navigation only | no action required | Compact header uses `‹ name (M/A) n/N cards ›`; the global running count appears only in row one. Card titles wrap to max two lines vs truncate in desktop; Board cards have no Edit/Delete action row | `view/board.rs`, `view/layout.rs`; seeded/archived/timer snapshots; layout tests |
| Board empty/filter-empty state | See contextual empty message and hints (`N/T`, `v`, or `n`) | keys as displayed | **❌ hints are not clickable** | Centered in available main area | `view/board.rs::draw_board`; snapshots `empty_board`, archived-only/all |
| Card detail | Close and return to Board; clears detail/history/fullscreen/scroll state | `q` / `Esc` | Click title `[ X ]` | Popup/fullscreen fills only the board content region | `app/detail.rs::detail_key`; detail update tests |
| Card detail | Toggle popup/fullscreen and re-anchor both histories to latest | `f` | Click title `[ □ ]` | Both geometries occupy the board content region; state and shortcut remain unchanged | `detail_toggle_rect`; `tests/update/detail.rs::card_detail_toggles_popup_and_fullscreen`, `card_detail_title_action_is_clickable` |
| Card detail | Switch focus between Comments and Runs | `Tab` | Click anywhere inside comments/runs section; wheel over a section also focuses it | Same | `DetailScrollTarget`; `tests/update/detail.rs::card_detail_scrolls_comments_and_runs_independently` |
| Card detail / Comments | Select focused comment, clamped (no wrap), and keep wrapped span visible | `↑/↓`, `k`/`j` | Click a rendered comment row; wheel raw-scrolls wrapped rows and pulls focus into viewport | Same, with less vertical room | `detail_nav`, `follow_comment_focus/scroll`; comment focus and wheel tests; `tests/mouse.rs::card_detail_comment_zones_*` |
| Card detail / Runs | Select run, clamped (no wrap), and keep row visible | `↑/↓`, `k`/`j` while Runs focused | **❌ no run-row click parity**; wheel over Runs scrolls and pulls selection into viewport; clicking section only changes section focus | Same | `follow_run_focus/scroll`; `tests/update/detail.rs::detail_runs_selection_moves_*`, wheel run test |
| Card detail | Edit card when Runs is focused (or comments absent/not focused) | `e` | **❌ no mouse parity** | Fullscreen edit form; returns to detail | `Form::card_edit(...).returning_to(CardDetail)`, `Effect::LoadFormOptions`; `e_edits_the_card_when_runs_focused` |
| Card detail / Comments | Edit focused user comment | `e` | Click `[ Edit ]` action | Form returns to detail; action bar only appears when focused/nonempty and section has ≥3 rows | `Form::comment_edit`; mouse zones; comment edit tests |
| Card detail / Comments | Delete focused user comment after confirmation | `d`, then confirmation keys | Click `[ Delete ]` opens confirmation, but **❌ yes/no mouse parity**; Compact `[ X ]` cancels | Fullscreen confirm | `Effect::CommentDelete`; `d_confirms_comment_delete_and_y_deletes_n_cancels_both_return_to_detail`; mouse tests |
| Card detail / Comments | View selected comment’s complete audit history | `h` | Click `[Hist]` | Opens a content-region History sheet | `Effect::LoadCommentHistory`; history tests/snapshots and mouse zone test |
| Card detail | Archive/restore this card | `a` | **❌ no mouse parity** | Same | shared `archive_card` → `Effect::CardArchive`; archive guard tests |
| Card detail | Add comment | `c` | **❌ no direct mouse parity** | Fullscreen one-field form; returns to detail | `Form::comment(...).returning_to(CardDetail)` → `Effect::CommentAdd`; form/update tests |
| Card detail | Focus exact selected run pane | `o` (works regardless of focused section) | **❌ no mouse parity** | Same | `Effect::FocusRun(card_id, run_id)`; extensive `tests/update/detail.rs::o_*` and `card_detail_o_*` |
| Card detail / awaiting card | Confirm successful completion | `Enter` only when status is `awaiting`; no-op for done/others | **❌ no mouse parity** | Same | `Effect::RunDone(id, Ok)`; awaiting detail tests/snapshots |
| Card detail | Cancel an actually open run, with confirmation | `x`, then confirmation | **❌ no action/yes-no mouse parity**; Compact `[ X ]` cancels | Content-region confirm | `Effect::RunCancel`; `tests/update/modals.rs::cancel_is_refused_when_no_run_is_open` |
| Card detail | Retry/relaunch card, with confirmation | `r`, then confirmation | **❌ no action/yes-no mouse parity**; Compact `[ X ]` cancels | Content-region confirm | `Effect::RunRetry`; retry modal tests |
| Card detail visible content | Read card id/title, status and awaiting reason, archived flag, harness/model/effort, permission/session/space, wrapped description, chronological comments, and runs (`#id harness · outcome/active · duration`) | selection/navigation above | no action required | Compact panel (`<60`) keeps card/run actions as named wrapped chips (`[ Edit ]`, `[ Archive ]`, optional `[ Confirm ]`, `[ Add ]`, `[ Open ] [ Retry ] [ Cancel ]`); sections stay closed and Runs reserves its action row before body rows | `view/detail.rs`; card-detail snapshots; run row tests |
| Card detail visible scroll state | Separate comments/runs scroll offsets and `↑`/`↓` section-title indicators; newest comment/run selected and bottom-visible on open | nav keys | wheel independently over each section | Same | `scroll_detail_to_latest`, `detail_section_title`; `card_detail_history_overflow_starts_latest_and_scrolls_sections` |
| Card/Column/Comment form | Move focus through visible fields, wrapping and skipping hidden fields | `Tab`; `Shift+Tab` / `BackTab` | **❌ no field-click parity** | Whole-field windowing keeps focus visible; vertical scrollbar if fields overflow | `forms/options.rs::focus_step`; `tests/update/forms.rs::form_field_cycling_wraps_and_skips_hidden`; layout/snapshot form-scroll tests |
| Forms / choice field | Cycle selected choice, wrapping; dependent fields/options rebuild | `←`/`↑` previous; `→`/`↓`/`Space` next | **❌ no choice mouse parity** | Same | `form_key`, `Field::cycle`; model/space/trigger/harness cascade tests |
| Forms / text field | Edit text with `tui_textarea` supported keys; `?` and `f`/`F` are literal in forms | normal text/cursor/edit keys except global-handled `Enter`, `Tab`, `Esc`; `?` and `f` insert text | **❌ no field-click or mouse text-edit parity** | Multiline description/system-prompt/comment values wrap, follow the cursor, and use the same strong focused/unfocused value treatment as title/name; `$EDITOR`, `Ctrl+J`, and `Shift+Enter` remain unchanged | `app/forms.rs`; `tests/update/modals.rs::question_mark_is_literal_text_inside_a_form`; `tests/update/forms.rs::f_types_into_*`; snapshots |
| Forms / picker field | Toggle the form between centered popup and content-region fullscreen; bound (and advertised in the title bar) only while focus is on a non-text picker field, so typing `f` can never resize the form | `f` (picker fields only) | **❌ no mouse parity** | Content-region fullscreen | `form_key`, `view/form.rs`; `tests/update/forms.rs::f_still_toggles_fullscreen_on_picker_fields`, `form_title_advertises_the_toggle_only_on_picker_fields`; `new_card_modal_picker_focused` snapshot |
| Forms / multiline field | Hand focused multiline description/system prompt/comment to external `$EDITOR`/`$VISUAL` (`vi` fallback), suspend and repaint TUI | `Ctrl+E` (no-op on non-multiline) | **❌ no mouse parity** | Same | `Effect::EditFocusedTextArea`; `editor.rs`; `tests/update/editor.rs::*` |
| Forms | Save/submit with preflight validation; return to opener on success | `Enter` | Click `[ Save ]` (all layouts) | Fullscreen; button bar remains bottom row | Effect depends on form kind: `CardCreate/Update`, `ColumnCreate/Update`, `CommentAdd/Update`; `tests/mouse.rs::form_bar_save_submits_and_bar_cancel_closes`; form tests |
| Forms | Cancel without submit; return to opener | `Esc` | Click `[ Cancel ]` (all layouts); Compact also `[ X ]` | Content-region fullscreen | `close_form`; mouse form test and modal return-to tests |
| Card create/edit form | Configure title, base-prompt description, harness, model/default/custom, effort, permission (when supported), Herdr session, workspace/new-workspace, space ref/custom ref, and cwd for new workspace | form controls above | Only Save/Cancel are clickable; **❌ field mouse parity** | Dynamic fields hidden, not omitted; long form windowed | `forms/builders.rs::build_card_fields`; `tests/forms.rs`, `tests/update/forms.rs` |
| Column create/edit form | Configure name, manual/auto trigger, auto-only system prompt, success/fail transition columns, fresh session, model/effort/harness/permission overrides, timeout minutes | form controls above | Only Save/Cancel clickable; **❌ field mouse parity** | Dynamic system-prompt/permission visibility; windowed | `column_fields_from_values`; column form tests and snapshots |
| Comment add/edit form | Edit multiline comment body; reject empty body | form controls / `Ctrl+E` | Save/Cancel only; **❌ text-field mouse parity** | Fullscreen | `FormKind::Comment/CommentEdit`; `comment_edit_form_*` tests |
| Picker (project, board, column, delete relocation) | Move selection clamped, choose, or cancel | `↑/↓ k/j`; `Enter`; `q`/`Esc` | Click a rendered row to select and activate it (item rows choose, action rows like `⇄ Other projects…`/`＋ New board` drill or open forms); Compact `[ X ]` cancels | Content-region fullscreen; wrapped whole-row window; desktop centered list | `app/picker.rs`, `view/overlays.rs::draw_picker`; picker snapshots; `tests/mouse.rs::picker_sheet_close_cancels_without_choosing`, `rendered_picker_rows_and_confirm_buttons_use_existing_reducers` |
| Confirm overlay | Accept destructive action or decline | accept `y`/`Enter`; decline `n`/`Esc` | **❌ no clickable yes/no parity**; Compact `[ X ]` declines | Content-region fullscreen; desktop centered | `app/confirm.rs`; confirm snapshots/modals/mouse close test |
| Compact Switcher / Columns | Select a current column; switch to the board picker; apply template | `↑/↓ k/j`, `Enter`; `q` closes; `Esc` closes | Click every visible row; click `[ X ]` | Compact-only/content-region sheet; column rows show card counts; trailing `⇄ Switch board →` row opens the **board picker** (the old second switcher level is gone — `b` and the header board chip open the same picker at every breakpoint) and `⊞ Apply template` below it | `app/switcher.rs`, `view/board.rs::draw_switcher`; `tests/update/switcher.rs::*`, `tests/mouse.rs::switcher_*` |
| MoveColumn mini-mode sheet | Stage/commit/discard reorder | `←/→ h/l`; `Enter`; `q`/`Esc` | **❌ stage/commit mouse parity**; Compact `[ X ]` discards only | Content-region instruction sheet; desktop centered | `app/move_column.rs`; move-column tests/snapshots |
| Comment History sheet | Read oldest→newest revision headers/body, including deletion marker; scroll clamped by wrapped rows; close to Card Detail | `↑/↓ k/j`; `q`/`Esc` | **❌ scroll mouse parity**; Compact `[ X ]` closes | Content-region sheet; desktop centered 90×24 max | `app/comment_history.rs`, `view/overlays.rs`; comment-history snapshots; mouse close test |
| Help | Read exhaustive binding table and scroll | `↑/↓ k/j`; Compact closes only `q`/`Esc`; Regular/Wide any non-nav key closes | **❌ scroll mouse parity**; Compact `[ X ]` closes; centered sheets expose `[ X ]` | Stacked bordered section cards (one column + scrollbar in Compact/Regular; two columns of cards with row scrolling on Wide), with no persistent hint row | `app/help.rs`, `view/overlays.rs`; `tests/help.rs` source-coverage tests; help snapshots/mouse close test |
| Any Compact `render_sheet_frame` sheet | Close/cancel/back using title `[ X ]` | equivalent `Esc` | Click `[ X ]` | Present only Compact; applies to forms, picker, confirm, help, switcher, MoveColumn, CommentHistory | `widgets::render_sheet_frame`, `Zone::SheetClose`; `tests/mouse.rs::*sheet_close*` |

## Linear mode

Linear mode is the board for a herdr space the work plugin has bound to a Linear project (see [`design.md`](design.md) section 13). It has its own screens and keys; the kanban rows above do not apply. The layout vocabulary differs: Linear mode has no Compact/Regular/Wide split. It stacks when the body is narrower than two 36-cell columns (72 cells), and the "Narrow behavior" column below describes that stacked layout. Every effect passes the Linear allow set in `driver/linear.rs::linear_allows`; tests below are in `tests/linear/mod.rs` unless named otherwise.

| Screen/context | Capability | Keyboard | Mouse | Narrow behavior | Backing effect / test evidence |
|---|---|---|---|---|---|
| Linear board | Focus previous/next group, clamped | `←`/`→`, `h`/`l` | Click a group header | One group fills the width; the keys move between groups | `app/linear.rs::board_key`, `click_group`; `a_click_on_a_column_header_selects_that_group_without_opening_anything` |
| Linear board | Focus previous/next card in the focused group, clamped | `↑`/`↓`, `k`/`j` | Wheel up/down | Same | `board_key`; `scrolling_moves_the_card_selection_and_stops_at_the_ends` |
| Linear board | Open the selected card's detail | `Enter` | Click a card; a card that moved since the draw is found again by identifier | Same | `click_card`; `a_click_on_a_card_selects_it_and_opens_its_detail`, `a_card_click_in_the_stacked_layout_opens_that_card`, `a_click_on_a_card_that_moved_since_the_draw_resolves_by_identifier` |
| Linear board, detail, not-bound, error | Refresh the snapshot and the space list; a refresh while one is in flight is dropped with a toast | `r` / `R` | **❌ no mouse parity** | Same | `Effect::LinearSnapshot`, `Effect::LinearList`; `a_refresh_while_one_is_in_flight_is_dropped_with_a_toast` |
| Linear board | Open the scrolling help sheet; it ends with the person's herdr keys when their herdr config has any | `?`; `↑`/`↓`/`k`/`j` scroll; any other key closes | **❌ no mouse parity** | Same | `linear_key`; `the_help_sheet_scrolls_to_a_herdr_section_built_from_the_config`, `a_missing_herdr_config_leaves_no_herdr_section` |
| Linear board | Quit | `q` / `Esc` | **❌ no mouse parity** | Same | `Effect::Quit` |
| Linear board, not-bound / strip | Focus the strip of spaces with no bound project; on the not-bound screen this space is its first row | `s` | Click a strip row (focuses and opens it) | Same | `strip_owned_key`; `s_then_down_then_enter_opens_the_space_picker_for_the_second_row` |
| Linear board, not-bound / strip | Switch the strip between unbound spaces and unmapped tabs | `t` | **❌ no mouse parity** | Same | `strip_owned_key`; `t_swaps_the_strip_to_todays_unmapped_tabs_and_back` |
| Linear board, not-bound / strip | Select a space; open the project picker for it; unfocus the strip | `↑`/`↓`/`k`/`j`; `Enter`; `Esc` | Click a strip row | Same | `strip_key`, `click_strip_row`; `a_click_on_a_strip_row_opens_the_space_picker_for_that_space`, `escape_from_the_strip_returns_focus_to_the_board_without_quitting`, `s_then_enter_on_the_not_bound_screen_binds_this_space_through_the_project_picker`, `a_click_on_a_strip_row_on_the_not_bound_screen_binds_that_space` |
| Linear board / strip | Read the strip state: loading, unavailable (with the reason), every space bound, or the rows with their ids | none | none | Same | `view/linear_strip.rs::strip_lines`; `a_failed_space_read_says_the_list_is_unavailable_not_that_every_space_is_bound`, `an_empty_space_list_says_every_space_is_bound`, `before_the_first_space_read_lands_the_strip_shows_a_loading_line` |
| Linear board, not-bound | Open the view picker for the bound project; with no bound project, toast instead | `v` | Click the header's view label | Same | `open_view_picker`; `v_on_the_board_opens_the_view_picker_for_the_bound_project`, `a_click_on_the_headers_view_opens_the_view_picker`, `the_view_picker_on_an_unbound_space_says_there_is_no_project_to_list_views_for` |
| Linear detail | Move the pane cursor | `↑`/`↓`, `k`/`j` | **❌ no mouse parity** | Same | `detail_key`; `detail_cursor_starts_on_the_working_pane_and_o_focuses_it` |
| Linear detail | Focus the selected pane | `o` | **❌ no mouse parity** | Same | `Effect::FocusPane`; `a_gone_pane_toasts_and_changes_nothing_else` |
| Linear detail | Open the issue in Linear | `u` | **❌ no mouse parity** | Same | `Effect::OpenIssueUrl`; `u_opens_the_issue_url_with_the_platform_opener` |
| Linear detail | Copy the worktree path | `y` | **❌ no mouse parity** | Same | `Effect::CopyWorktreePath`; `y_copies_the_worktree_path_and_says_when_the_directory_is_gone` |
| Linear detail | Bind the selected worktree binding for this issue, in its worktree; only `proposed`, `stale` or `misplaced` bindings are sent, every other state toasts its reason | `b` | **❌ no mouse parity** | Same | `bind_detail_card` → `Effect::BindHandoff`; `b_on_a_proposed_binding_sends_a_handoff_with_its_directory_and_the_issue`, `b_acts_on_stale_and_misplaced_bindings_too`, `b_on_a_bound_binding_toasts_that_it_is_already_bound_and_sends_nothing` |
| Linear detail | Back to the board | `q` / `Esc` | **❌ no mouse parity** | Same | `detail_key` |
| Linear not-bound | Quit; `Esc` unfocuses the strip first when it has focus | `q` / `Esc` | **❌ no mouse parity** | Same | `linear_key`; `escape_on_the_not_bound_strip_unfocuses_it_and_a_second_escape_quits` |
| Linear error | Retry; dismiss to the last good snapshot; quit | `r` / `R`; `Esc`; `q` | **❌ no mouse parity** | Same | `a_failed_refresh_keeps_the_last_good_snapshot_behind_the_error`, `a_first_fetch_failure_has_no_last_good_and_no_dismiss` |
| Linear stale daemon | Retry; quit | `r` / `R`; `q` / `Esc` | **❌ no mouse parity** | Same | `stale_daemon_screen_names_the_method_and_the_stop_command` |
| Linear picker (space, project, view) | Filter rows by case-insensitive substring; every printable key, including `?`, `r` and `q`, is filter text | type; `Backspace` | **❌ no mouse parity** | Same | `app/linear_picker.rs::linear_picker_key`; `typing_narrows_the_list_and_clamps_the_selection`, `question_mark_r_and_q_are_literal_while_filtering` |
| Linear picker | Move and choose; choosing a project for a strip space or a view starts a bind handoff | `↑`/`↓`; `Enter` | Click a row | Same | `Effect::BindHandoff`; `a_click_inside_an_open_picker_chooses_the_row_under_the_pointer`, `choosing_a_space_then_a_project_sends_one_handoff_with_both_ids_and_no_names`, `choosing_a_view_sends_a_handoff_with_space_project_and_view_ids` |
| Linear picker | Clear the filter, then close | `Esc`, `Esc` | **❌ no mouse parity**; a click outside a row is swallowed | Same | `escape_clears_the_filter_and_a_second_escape_closes_the_picker`, `a_click_inside_an_open_picker_does_not_reach_the_board_behind_it` |
| Linear picker | Read the list state: loading, rows, empty, unavailable, or read failed | none | none | Same | `a_picker_opened_before_its_list_arrives_shows_the_loading_line_then_the_rows`, `an_empty_list_an_unavailable_list_and_a_failed_read_render_differently` |

## Effects and user-visible result paths

| Screen/context | Capability | Keyboard | Mouse | Compact behavior | Backing effect / test evidence |
|---|---|---|---|---|---|
| All client I/O | A failed read or mutation is non-fatal and appears as a red transient toast above the action rail; app remains usable | whichever action emitted effect | whichever action emitted effect | Same, truncated to terminal width | `Driver::guard`; `tests/snapshots.rs::toast_on_client_error`; every dispatch/load client call routes through `guard` except pane title |
| Successful mutation refresh policy | Board writes refetch board; card update/archive refetch board then detail; comment add detail then board; comment update/delete detail only; run cancel/retry/done load card then board | action-specific | action-specific | Same | `driver/dispatch.rs::{After,mutate}` |
| Board refresh | Fetch current board and clamp column/card selection after shrink | `r/R`, daemon event, post-write | none | Same | `Driver::refetch`, `clamp_selection` |
| Board switch | Fetch selected board, reset modal/detail/drag/scroll selection state, preserve current card filter, update pane title | `b` picker flow | board chip click | Board picker at every breakpoint | `switch_board`, `App::replace_board`; `board_picker_loads_and_switch_preserves_filter` |
| Project list load | Refresh the `app.projects` cache and current project from `project.list` | on picker open / context change | — | — | `Effect::LoadProjects` → `refresh_projects`; `tests/update/scope.rs::*picker*` |
| Project picker load | Fetch `project.list` and open the project picker (current, recents, alphabetical, Global last, `＋ New project`) | `p` | project chip click | Same at every breakpoint | `Effect::LoadProjectPicker` → `load_project_picker`; `tests/update/scope.rs::p_opens_project_picker_with_current_project_first` |
| Board picker load | Fetch `project.list` and open the board picker for one project (`None` = current): selected/first board, recents, alphabetical, `⇄ Other projects…`, `＋ New board` | `b`; switcher “Switch board” row; after a project choice | board chip click | Same at every breakpoint | `Effect::LoadBoardPicker { project_id }` → `load_board_picker`; `tests/update/scope.rs::b_opens_board_picker_and_other_projects_drills_to_the_project_picker` |
| Select project | `project.select`: switch the context to a project and one of its boards, landing on that board's snapshot | project picker `Enter` | project row click | — | `Effect::SelectProject { project_id, board_id }`; `tests/update/scope.rs::choosing_another_project_opens_its_board_picker_and_selects_only_on_board_enter` |
| Select board | `board.select`: switch the context to a board (and its project), landing on its snapshot | board picker `Enter` | board row click | — | `Effect::SelectBoard(board_id)`; `tests/update/support_nav.rs::board_picker_loads_and_switch_preserves_filter` |
| Project create | `project.create`: create a project for an existing directory and land on its `main` board (auto-selected) | `＋ New project` form | picker row click | — | `Effect::ProjectCreate(ProjectCreateParams)`; `tests/update/scope.rs::project_create_form_submission_emits_project_create` |
| Board create | `board.create`: create a named board in a project and land on it (auto-selected) | `＋ New board` form | picker row click | — | `Effect::BoardCreate(BoardCreateParams)`; `tests/update/scope.rs::picker_new_project_and_new_board_rows_open_the_create_forms` |
| Move columns load | Fetch the move form's destination board and populate its Column field; changing the board reloads the options; the move never touches selection/recency | move form | — | — | `Effect::LoadMoveColumns { board_id }`; `tests/update/scope.rs::*move*` |
| Form option loading | Fetch capabilities + harness list; card forms also sessions and session-scoped spaces. Harness/session changes reload options | opening form / cycling driving choice | **❌ no mouse choice parity** | Same | `Effect::LoadFormOptions`; form-loading/cascading tests |
| Form metadata degraded mode | Capabilities failure: `capabilities unavailable (...); free-text`; spaces failure: `spaces unavailable (...); free-text`; harness/session list failures silently retain minimal defaults | form open/change | none | Footer toast | `Driver::load_form_options`; `fetch_failure_falls_back_to_free_text` |
| Form validation errors | Keep form open and toast: required `title`/`name`; `comment is empty`; card-space core validation (new workspace requires name/ref and cwd); invalid column permission preflight | `Enter` | Save click | Same | `Form::submit/preflight`; form validation tests |
| Archive/restore | Reject archiving busy/awaiting card using core validator; restoration allowed; success toasts `card archived` / `card restored` | `a` | **❌** | Same | shared `archive_card`; `archive_shortcut_rejects_busy_card`, archive guard tests; dispatch success toast |
| Card move guard | Archived selected card cannot use `m`, `H/L`, or start drag; toast `restore archived card before moving` | move keys | drag attempt | Same | `reject_archived_move`; archived move test |
| Move destination errors | Same-board move with no alternate column: `no other column to move cards to`; selected destination board with no columns: `<board> has no columns` | `m`/picker | **❌ picker mouse parity** | Same | `open_move_picker`, `load_columns_for_move`; snapshots/update tests |
| Delete column guards | Active run in column: core error toast (no picker); populated sole column: `no other column to move cards to`; hidden archived cards are included in relocation count | `D` | **❌** | Same | `delete_column`; delete-column tests/snapshots |
| Template guard | Nonempty board toasts `template only applies to an empty board`; no effect | `T` | Compact switcher row | Compact row remains open | `apply_template`; switcher/support-nav tests |
| Manual refresh acknowledgement | `refreshed` yellow/non-error toast appears before refetch; subsequent fetch error may replace it | `r/R` | **❌** | Same | `board_key`; refresh tests |
| Immutable system comments | Edit/delete action toasts `system comments are immutable`; history remains available; Edit/Delete remain clickable and deliberately route to same toast | `e`/`d` | `[ Edit ]`/`[ Delete ]` | Same if bar fits | system comment tests; `draw_comment_actions` |
| Detail not-ready/no-run errors | `o` before detail loaded: `card detail has not loaded yet`; loaded card with no runs: `this card has no run to jump to` | `o` | **❌** | Same | `detail_key`; `o_without_a_loaded_detail_toasts_*` |
| Cancel unavailable | `x` with no open run: `this card has no open run to cancel`; no confirm | `x` | **❌** | Same | `has_open_run`; modal cancel test |
| Focus run without Herdr | Toast `jump to pane requires Herdr (HERDR_SOCKET_PATH is unset)` | `o` | **❌** | Same | `Driver::focus_run` |
| Focus exact pane succeeds | Recorded pane focus sets `should_quit`, stepping TUI aside | `o` | **❌** | Same | `RunFocusAction::FocusedRecordedPane`; detail focus test |
| Focus rescue succeeds | Stay in TUI and yellow-toast exact run, reopened pane id, harness where applicable, and “ephemeral, not tracked as a run” | `o` | **❌** | Same | `focus_rescue_toast`; `o_on_a_run_whose_pane_is_gone_*` |
| Focus run fails | Red toast prefixes daemon diagnosis with `run #<id>:`; does not quit | `o` | **❌** | Same | `Driver::focus_run`; reopen/harness failure tests |
| Pane title integration | At startup/switch/filter change, Herdr plugin pane title is `Board [scope · FILTER]`; in Linear mode, after each good snapshot, it is `Linear: <project name, else space label, else space id>`; outside plugin context no-op; rename failures are intentionally swallowed and never toast; the daemon strips control and format characters again before the rename | `v`/board switch; Linear `r` / `R` | Compact board switch | Scope label sanitizes brackets/control chars and truncates; the Linear name drops brackets, control and format characters | `set_pane_title`, `view::pane_title`, `Effect::SetLinearPaneTitle`, `view::linear_pane_title`; `tests/update/pane_title.rs::*`, scope snapshots |
| Linear bind handoff | Start the plugin's bind in a new unfocused `bind` tab, then focus the pane the daemon returns; one handoff at a time; on success the header says the bind started and names `r`; a failure toasts `bind failed: <reason>`; a timeout tells the person to look for a `bind` tab before trying again; without a herdr socket it toasts and sends nothing | strip project choice, view choice, detail `b` | picker row click | Same | `Effect::BindHandoff` → `linear.bind_handoff`, then `Effect::FocusPane`; `tests/linear/mod.rs::after_a_successful_handoff_the_board_focuses_the_returned_pane`, `while_a_handoff_is_in_flight_the_picker_says_so_and_a_second_enter_sends_nothing`, `a_handoff_timeout_names_the_bind_and_its_own_limit_not_the_refresh_key`, `a_handoff_without_a_herdr_socket_toasts_and_sends_nothing` |
| External editor error | Editor spawn/read failure restores terminal then red-toasts error; successful return trims one trailing newline and requests full repaint | `Ctrl+E` | **❌** | Same | `editor.rs`, `Driver::edit_focused`; editor tests |
| Toast presentation | Error = white on red; success/info = black on yellow; transient toast content is truncated; expires when `now - at > 4` | none | none | Same | `view/overlays.rs::draw_footer`; `Driver::expire_toast`; toast snapshots |
| Cosmetic/empty-effect no-ops | Empty picker Enter; no selected card/column; detail `d/h` with no comments; Enter on non-awaiting detail; edge-clamped reorder; drag dropped at origin all emit nothing and do not panic | relevant keys | relevant mouse | Same | modal/detail/scope tests (`enter_on_an_empty_picker_does_not_panic`, `d_and_h_are_noops_with_no_comments`, etc.) |

## Mouse-parity summary

Mouse support is intentionally narrow rather than a full alternative input path. Full or useful parity exists for visible board/card focus, board card scrolling, detail popup/fullscreen, detail comment selection/edit/delete/history, form Save/Cancel, Compact sheet close, and Compact switcher rows. Board-card Edit/Delete are intentionally absent; the keyboard `e`/`d` paths remain. The largest explicit parity gaps are:

1. No mouse activation for Regular/Wide template application (board/project switching is clickable via the header `Board:`/`Project:` chips in every layout).
2. No mouse entry controls for create/edit/archive/delete/move/filter/refresh/run actions on the Board; Card Detail/comments retain their semantic Edit/Delete controls.
3. Confirmation yes/no choices are never clickable (picker rows are clickable and activate through the same `Enter` reducer).
4. Form fields cannot be focused, edited, or cycled with the mouse (only Save/Cancel).
5. Runs cannot be selected by clicking their rows; `o` has no click action.
6. Help and Comment History do not wheel-scroll.
7. Compact’s one-column board makes cross-column card/column drag effectively unavailable; keyboard `H/L` and `M` remain the routes.
8. Card Detail closes through its clickable title-row `[ X ]` control in every layout, matching the framed sheets.
